      - name: push
        run: |
          git fetch
          git pull --rebase
          CURRENT=$(git branch --show-current)
          echo Current branch - $CURRENT
          git push origin $CURRENT:test
